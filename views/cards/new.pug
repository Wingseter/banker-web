extends ../layout

block content
  .container
    h1.main-title New Card
    form(action="/cards", method="POST")
      include _form
      button.btn.btn-outline-primary(type="submit") Make new Card

    