extends ../layout

block content
  .container
    h1.main-title Edit Account Information
    form(action=`/accounts/${account.id}?_method=PUT`, method='POST')
      include _form
      button.btn.btn-outline-primary(type="submit") Update information
